name: srcom Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Build with Dagger
        uses: dagger/dagger-for-github@v8.4.0
        with:
          verb: call
          args: build --source=. export --path=./dist/srcom

      - name: Package and Checksum
        run: |
          cd dist
          mv srcom srcom-${{ github.ref_name }}-linux-amd64
          sha256sum srcom-${{ github.ref_name }}-linux-amd64 > srcom-${{ github.ref_name }}-linux-amd64.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          draft: false
          prerelease: false
